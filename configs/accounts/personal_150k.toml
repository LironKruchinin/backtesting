# Personal cash futures account, $150000 baseline
#
# An ACCOUNT, as data. Pairs with a strategy config (`configs/*-combo.toml`) to
# form one evaluation identity; the methodology, the engine capture contract and
# the metric battery live in `docs/ACCOUNT_EVAL_SPEC.md`. Nothing here describes
# execution costs — those stay in the strategy config's named `[execution]` fill
# model (CLAUDE.md §2.4), because an account rule and a cost assumption are
# different claims and must not share a field.
#
# Money is a decimal STRING in USD (D-0027); the loader parses it to
# `_nano_usd` integers. Percentages are f64 on a 0-100 scale (§4).
#
# VERIFICATION: there is no firm and there are no firm rules to verify. The one
# number a cash account would need from an outside authority is the margin
# requirement, and `[margin]` carries hand-fetched CME maintenance margins
# (accessed 2026-07-30).
#
# The control arm. No firm, no profit target, no threshold, nothing to breach
# -- which is the whole point of keeping these beside the prop files: every
# metric that differs between `personal_150k` and a prop account of the same
# size, on the same strategy and the same window, is attributable to the
# ACCOUNT RULE and nothing else. That is the comparison the prop numbers only
# mean something inside of.
#
# `[ruin]` replaces breach probability. `[margin]` was populated by hand on
# 2026-07-30 from CME's SPAN2 outright export, so the contract-count cap
# is modelled rather than unmodelled.

schema_version = 1

[account]
id = "personal_150k"
firm = "none"
product = "self-funded cash account"
phase = "cash"
kind = "cash"
nominal_size_usd = "150000"
starting_balance_usd = "150000"

# Whether this project's output -- an automated strategy -- may legally be
# run on this account. Closed set: forbidden | conditional | unknown.
# `unknown` is a VALUE, not a placeholder to be guessed away: it means no
# stance was found on the firm's own pages, and it must not be read as
# permission. Cited like everything else in `[source]`.
automation_policy = "unknown"
automation_policy_source_url = "https://www.cmegroup.com/solutions/risk-management/performance-bonds-margins.html"
automation_policy_accessed_date = "2026-07-30"
# There is no firm, so there is no firm policy to cite. What
# governs an automated strategy here is the chosen BROKER's own
# automated-trading and API terms, and no broker is named in
# this config -- so nothing was verified and the value is
# `unknown`. It becomes verifiable the day a broker is chosen.
# The cited URL is the only outside authority this file has and
# it is about margin, not automation; it is named because the
# field's citation discipline is not waived just because the
# answer is unknown.

# TRUE means the firm's own rules forbid running this project's output on
# the funded account this evaluation leads to. It does NOT mean excluded:
# a reference-only account is evaluated by the full battery, because its
# rule STRUCTURE is what measures strategy fragility, which is the
# research question. Which product to actually target is an M4 decision,
# taken against rules re-verified at that time -- they will have changed.
reference_only = false

[source]
rule_pages = [
  "https://www.cmegroup.com/solutions/risk-management/performance-bonds-margins.html",
]
accessed_date = "2026-07-30"
page_last_updated = "unstated"

[session]
# The firm's trading day, which is NOT the exchange session. Daily PnL is
# sliced on this day boundary via `crucible-data::calendar` (§4 pins fold
# windows in trading days for the same reason a wall-clock day is wrong
# here: 18:00 CT Monday is Tuesday's session).
calendar_id = "cme_globex_equity_index"

[position_limit]
scaling = "none"
# No firm-imposed cap. The real cap is margin, which is
# unmodelled (see `[margin]`), so `max_contracts` is absent:
# size comes from the strategy config's `qty_contracts` and
# the report must say that no margin cap was checked.

[ruin]
# A cash account cannot be 'breached' -- there is no firm to close
# it -- so risk-of-ruin replaces breach probability (ACCOUNT_EVAL_SPEC
# §4.5). This threshold is OUR pre-declared research parameter, not
# anybody's rule, and every report that quotes a ruin probability must
# print the threshold beside it.
threshold_cum_pnl_pct = -50.0
basis = "equity_including_unrealized"

[margin]
# POPULATED 2026-07-30, BY HAND. Source: CME Group **SPAN2 outright
# export** (`margins-span2-outright.xlsx` and companion CSVs), fetched by
# a human on 2026-07-30. It could not be fetched any other way: CME's
# Data Terms of Use forbid automated retrieval and their site blocks it
# outright, so no automated process may refresh these. They change by
# clearing advisory several times a year -- re-fetch by hand and move
# `as_of_date` when they do.
#
# WHICH NUMBER THIS IS: the EXCHANGE MAINTENANCE margin, the conservative
# baseline. Broker day-trading margins are far smaller and are a future
# refinement, not the baseline -- where the two differ the conservative
# (larger) figure wins, because it caps position size lower and so
# understates rather than flatters what the account could hold.
#
# BOTH SIDES ARE KEPT, NOT PRE-FLATTENED. Where the export splits long
# from short, both are recorded and the conservative rule picks the
# larger side *for the position's direction* at evaluation time. Storing
# only the max would be a smaller file and worse data: it would answer a
# question about a short with a number derived from a long.
#
# INITIAL MARGIN IS NOT SOURCED. The SPAN2 outright export carries
# maintenance only. `initial_*_usd` below is **1.1 x maintenance**, the
# CME speculator convention -- `initial_basis` says so on every row. It
# is a CONVENTION, NOT A MEASUREMENT, and any result that turns on it
# must say so until a sourced figure replaces it.
#
# CLEARING CODES: CME exports key on clearing code, and Globex symbols
# differ. `EC` = 6E (Euro FX); `21` = ZN (10-Year T-Note). Recorded so
# the next hand-fetch can find the same rows.
#
# MICROS: MES/MNQ/M2K/MCL are present, which is what lets the 25k tier
# size in micros rather than being unable to hold one mini. MGC and M6E
# were ABSENT from the exports -- that is an optional future row, NOT a
# gap in what was fetched. Nothing was dropped.

[[margin.product]]
symbol = "ES"
name = "E-mini S&P 500"
contract_month = "2026-09"
maintenance_long_usd = "25145"
maintenance_short_usd = "22554"
initial_long_usd = "27659.50"
initial_short_usd = "24809.40"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"

[[margin.product]]
symbol = "MES"
name = "Micro E-mini S&P 500"
contract_month = "2026-09"
maintenance_long_usd = "2514"
maintenance_short_usd = "2255"
initial_long_usd = "2765.40"
initial_short_usd = "2480.50"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"

[[margin.product]]
symbol = "NQ"
name = "E-mini Nasdaq-100"
contract_month = "2026-09"
maintenance_long_usd = "37539"
maintenance_short_usd = "35687"
initial_long_usd = "41292.90"
initial_short_usd = "39255.70"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"

[[margin.product]]
symbol = "MNQ"
name = "Micro E-mini Nasdaq-100"
contract_month = "2026-09"
maintenance_long_usd = "3754"
maintenance_short_usd = "3569"
initial_long_usd = "4129.40"
initial_short_usd = "3925.90"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"

[[margin.product]]
symbol = "RTY"
name = "E-mini Russell 2000"
contract_month = "2026-09"
maintenance_long_usd = "10656"
maintenance_short_usd = "10145"
initial_long_usd = "11721.60"
initial_short_usd = "11159.50"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"

[[margin.product]]
symbol = "M2K"
name = "Micro E-mini Russell 2000"
contract_month = "2026-09"
maintenance_long_usd = "1066"
maintenance_short_usd = "1015"
initial_long_usd = "1172.60"
initial_short_usd = "1116.50"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"

[[margin.product]]
symbol = "CL"
name = "Crude Oil (WTI)"
contract_month = "2026-09"
maintenance_long_usd = "8220"
maintenance_short_usd = "7909"
initial_long_usd = "9042.00"
initial_short_usd = "8699.90"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"

[[margin.product]]
symbol = "MCL"
name = "Micro Crude Oil (WTI)"
contract_month = "2026-09"
maintenance_long_usd = "824"
maintenance_short_usd = "793"
initial_long_usd = "906.40"
initial_short_usd = "872.30"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"

[[margin.product]]
symbol = "GC"
name = "Gold"
contract_month = "2026-08"
# The export gives ONE maintenance figure, not a long/short split, so
# both sides carry the same value -- equal because the source says so,
# not because the split was averaged away. 07/2026 read $20,174 and is
# expiring; 08/2026 is the live front month and is what is encoded.
maintenance_long_usd = "20182"
maintenance_short_usd = "20182"
initial_long_usd = "22200.20"
initial_short_usd = "22200.20"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"

[[margin.product]]
symbol = "6E"
name = "Euro FX"
clearing_code = "EC"
# Banded 08-09/2026: the export quotes one figure across both months.
contract_month = "2026-08"
maintenance_long_usd = "2100"
maintenance_short_usd = "2100"
initial_long_usd = "2310.00"
initial_short_usd = "2310.00"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"

[[margin.product]]
symbol = "ZN"
name = "10-Year T-Note"
clearing_code = "21"
# Banded 09/2026-03/2027: the export quotes one figure across the band.
contract_month = "2026-09"
maintenance_long_usd = "1875"
maintenance_short_usd = "1875"
initial_long_usd = "2062.50"
initial_short_usd = "2062.50"
initial_basis = "1.1x_maintenance_cme_speculator_convention_not_sourced"
as_of_date = "2026-07-30"
